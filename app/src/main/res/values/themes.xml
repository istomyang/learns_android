<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- !!!!! 目前不考虑黑暗模式，这边修改需要同步过去 !!!!! -->

    <!-- Base application theme. -->
    <!--  heme.MaterialComponents.DayNight.DarkActionBar  -->
    <style name="Theme.Learns_android" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor" >@color/primaryColor</item>
<!--                <item name="android:statusBarColor" tools:targetApi="l">?attr/backgroundColor</item>-->
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Learns_android.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Learns_android.NoActionBar.StatusBarHey">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor" >@color/white</item>
    </style>
</resources>